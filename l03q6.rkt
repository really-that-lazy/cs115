;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-beginner-reader.ss" "lang")((modname labinterface03) (read-case-sensitive #t) (teachpacks ()) (htdp-settings #(#t constructor repeating-decimal #f #t none #f () #f)))


;; Lab 03, Question 6

(define (costByMinute length costPerMinute)
	(* length costPerMinute))

(define (reelsPastCutoff num-clips breakpoint1)
	(


(define (film-choice num-clips length breakpoint1 discount1 breakpoint2 discount2)
  ...)
